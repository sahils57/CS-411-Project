Hey guys, be sure to install these with pip before running or it won't work.

pip install flask
pip install python-twitter

then all you have to do is run app.py in your command line (bash, terminal, etc)
> python app.py

